
{% extends 'base.html.twig' %}

{% block title %}Facture index{% endblock %}

{% block body %}

    <br>
    <div class="container-fluid dashboard-div">
        <div class="row top-actions">
            <div class="col-8 top-header">
                <h4>Liste des factures</h4>
            </div>
{#            <div class="col-4 actions text-right">#}
{#                <a class="btn btn-primary" href="{{ path('facture_new') }}"><i class="fas fa-plus"></i></a>#}
{#            </div>#}
        </div>

        <br>
        <div class="card-body">
            <table id="facture-table" class="table table-bordered table-hover">
                <thead>
                <tr>
                    <th>Client</th>
                    <th>Date Facturation</th>
                    <th>Numero</th>
                    <th>actions</th>
                </tr>
                </thead>
                <tbody>
                {% for facture in factures %}
                    <tr>
                        <td>{{ facture.utilisateur }}</td>
                        <td>{{ facture.dateFacturation ? facture.dateFacturation|date('d-m-Y') : '' }}</td>
                        <td>{{ facture.numero }}</td>
                        <td>
                            <a class="btn btn-info" href="{{ path('facture_show', {'id': facture.id}) }}"><i class="fas fa-eye"></i></a>
{#                            <a class="btn btn-warning" href="{{ path('facture_edit', {'id': facture.id}) }}"><i class="fas fa-edit"></i></a>#}
{#                            {{ include('facture/_delete_form.html.twig') }}#}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7">Aucun enregistrement trouvé</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
