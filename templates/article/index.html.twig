{% extends 'base.html.twig' %}


{% block title %}Article index{% endblock %}

{% block body %}
    <br>
    <div class="container-fluid">
        <div class="row top-actions">
            <div class="col-8 top-header">
                <h4>Liste des Articles</h4>
            </div>
            <div class="col-4 actions text-right">
                <a class="btn btn-primary" href="{{ path('article_new') }}"><i class="fas fa-plus"></i></a>
            </div>
        </div>

        <br>
{#        <div>#}
{#            {% if(errorMsg != " ") %}#}
{#                <div class="alert alert-warning" role="alert">#}
{#                    <i class="fas fa-exclamation-triangle"></i> {{ errorMsg }}#}
{#                </div>#}
{#            {% endif %}#}

{#        </div>#}
        <div class="card-body">
            <table id="article-table" class="table table-bordered table-hover">
                <thead>
                <tr>
                    <th>Designation</th>
                    <th>Prix</th>
                    <th>actions</th>
                </tr>
                </thead>

                {% for article in articles %}
                    <tr>
                        <td>{{ article.designation }}</td>
                        <td>{{ article.prix }}</td>
                        <td>
                            <a class="btn btn-info" href="{{ path('article_show', {'id': article.id}) }}"><i class="fas fa-eye"></i></a>
                            <a class="btn btn-warning" href="{{ path('article_edit', {'id': article.id}) }}"><i class="fas fa-edit"></i></a>
                            {{ include('article/_delete_form.html.twig') }}
                        </td>
                    </tr>

                {% else %}
                    <tr>
                        <td colspan="4">Aucun enregistrement trouvé</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}











